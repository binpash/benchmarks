# Verify benchmarks
# ~ Compare hash values (md5, sha256)
# ~ Report running time and compare with expected running time
# ~ Compare result of -x: (take into consideration the appearance of the user’s filesystem)
# ~ [later] use strace
